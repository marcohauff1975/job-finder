venv/
__pycache__/
*.pyc
.pytest_cache/

# runtime state - never commit user data, credentials, or the auth database
.env
data/
users/
history/
resume/

# legacy per-user config, superseded by data/auth.db - would contain password hashes if it existed
config/auth_config.yaml*
auth.py.bak*

# unused duplicate of .streamlit/config.toml
config.toml

# generated by ux_reviewer's inspection tool - screenshots and its fixture resume
req2prod/ux_reports/
